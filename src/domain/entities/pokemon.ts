export interface Pokemon{
    id: number;
    name: string;
    type: string[]; // fire, water, insect
    avatar: string; // foto principal
    sprites: string[]; // fotos 

    //TODO caracteristicas adicional
}